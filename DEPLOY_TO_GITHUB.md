# GitHub Pages 部署指南

## 步驟 1: 創建 GitHub 倉庫

1. 登錄 [GitHub](https://github.com)
2. 點擊右上角的 **+** → **New repository**
3. 輸入倉庫名稱（例如：`time-learning-game`）
4. 選擇 **Public** 或 **Private**
5. 點擊 **Create repository**

## 步驟 2: 上傳文件

### 方法 A: 通過 GitHub 網頁上傳（簡單）

1. 在新創建的倉庫頁面，點擊 **uploading an existing file**
2. 將以下文件拖拽到上傳區域：
   - `tmp.html`
   - `package.json`
   - `README.md`
   - `.gitignore`
3. 滾動到頁面底部，在 **Commit changes** 框中輸入訊息（例如："Initial commit"）
4. 點擊 **Commit changes**

### 方法 B: 通過 Git 命令行（推薦）

```bash
# 初始化 Git 倉庫
cd "c:/Users/Simon/Coding/Math Game/clock/寻找童趣游戏字体—1770883878759"
git init
git add .
git commit -m "Initial commit: Time Learning Game"

# 連接到 GitHub 倉庫
git branch -M main
git remote add origin https://github.com/你的用戶名/倉庫名.git
git push -u origin main
```

**注意：** 將 `你的用戶名/倉庫名` 替換為實際的 GitHub 用戶名和倉庫名。

## 步驟 3: 啟用 GitHub Pages

1. 在倉庫頁面，點擊頂部的 **Settings** 標籤
2. 在左側菜單中找到 **Pages**
3. 在 **Build and deployment** 部分：
   - **Source**: 選擇 **Deploy from a branch**
   - **Branch**: 選擇 **main** (或 `master`) 分支
   - **Folder**: 留空（使用根目錄）
4. 點擊 **Save**

## 步驟 4: 等待部署

- GitHub 會自動開始部署
- 通常需要 1-3 分鐘
- 部署完成後，在 Pages 設置頁面會顯示：
  ```
  Your site is live at:
  https://你的用戶名.github.io/倉庫名/
  ```

## 步驟 5: 更新部署（如需修改遊戲）

當你修改遊戲後，只需：

```bash
git add .
git commit -m "更新遊戲功能"
git push
```

GitHub Pages 會自動重新部署（約 1-2 分鐘）。

## 自定義域名（可選）

如果你想使用自定義域名：

1. 在 Pages 設置中，找到 **Custom domain**
2. 輸入你的域名（例如：`game.yourdomain.com`）
3. 按照指示配置 DNS 記錄

## 常見問題

**Q: 部署後無法訪問？**
- 檢查倉庫設置中 Pages 是否啟用
- 等待幾分鐘，部署可能需要時間
- 確認倉庫是 Public 的（如果是 Private 可能無法訪問）

**Q: 如何檢查部署狀態？**
- 倉庫頁面 → **Actions** 標籤
- 查看最近的 workflow run 狀態

**Q: 如何使用不同的主頁文件名？**
- 將 `tmp.html` 重命名為 `index.html`
- GitHub Pages 會自動將 `index.html` 作為主頁

## 推薦：重命名主文件（可選）

為了更好的訪問體驗，建議將 `tmp.html` 重命名為 `index.html`：

```bash
cd "c:/Users/Simon/Coding/Math Game/clock/寻找童趣游戏字体—1770883878759"
ren tmp.html index.html
```

或直接在文件管理器中重命名，然後提交更改：

```bash
git add .
git commit -m "重命名主文件為 index.html"
git push
```

這樣訪問 `https://你的用戶名.github.io/倉庫名/` 會直接顯示遊戲，無需加上 `tmp.html`。

## 完成！🎉

現在你可以分享 GitHub Pages 的 URL 給學生們測試遊戲了！
